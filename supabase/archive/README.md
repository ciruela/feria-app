# Migraciones archivadas

Scripts one-off o ya aplicados en producción. **No volver a ejecutar** salvo que sepas exactamente por qué.

- `006_world_guns_tenant.sql` — migró el tenant `default` a `world-guns` en prod.
